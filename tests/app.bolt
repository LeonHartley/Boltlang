module app;

obj User {
    id int
    username string
    password string
}

func functionOne(user User) {
    log("kek");
}

func functionTwo(user User) {
    if(user.id == 0) {
        log("user id is not set", .error)
        return;
    }

    log("user id is set", .info);
}
